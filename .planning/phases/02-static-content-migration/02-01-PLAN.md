---
phase: 02-static-content-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/assets/mascot.png
  - src/assets/logo-white.png
  - src/styles/globals.css
  - messages/es.json
  - messages/en.json
  - src/app/[locale]/page.tsx
autonomous: true

must_haves:
  truths:
    - "messages/es.json contains all ~64 translation keys from the original LanguageContext, restructured into nested landing.* format"
    - "messages/en.json has identical key structure to es.json with English translations"
    - "lucide-react is installed and importable"
    - "Mascot and logo images exist in src/assets/"
    - "page.tsx imports and composes all 11 section components in correct order"
  artifacts:
    - path: "messages/es.json"
      provides: "Complete Spanish translations for all 11 landing page sections"
      contains: "landing.hero.eyebrow"
    - path: "messages/en.json"
      provides: "Complete English translations for all 11 landing page sections"
      contains: "landing.hero.eyebrow"
    - path: "src/assets/mascot.png"
      provides: "Tardi mascot image for Hero section"
    - path: "src/assets/logo-white.png"
      provides: "White logo for Navbar and Footer"
    - path: "src/app/[locale]/page.tsx"
      provides: "Landing page composition with all 11 sections"
      contains: "AnnouncementBanner"
  key_links:
    - from: "messages/es.json"
      to: "messages/en.json"
      via: "Identical key structure"
      pattern: "landing\\.hero\\.eyebrow"
    - from: "src/app/[locale]/page.tsx"
      to: "src/app/[locale]/_components/*"
      via: "Named imports from _components directory"
      pattern: "from \"\\./_components/"
---

<objective>
Set up the translation infrastructure, static assets, and page entry point for the complete landing page migration.

Purpose: Establish the foundation that all 11 section components depend on -- translation files with all ~64 keys, image assets, CSS utilities, and the page.tsx that composes everything together.
Output: Complete es.json and en.json message files, copied image assets, lucide-react installed, font-mono-accent utility added, and page.tsx ready to render all sections.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-content-migration/02-RESEARCH.md

# Current files to modify
@src/styles/globals.css
@src/app/[locale]/page.tsx
@src/app/[locale]/layout.tsx
@messages/es.json
@messages/en.json

# Original SPA source for translation extraction
@/home/hybridz/Projects/deep-tech-nexus/src/contexts/LanguageContext.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/pages/Index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react, copy assets, add CSS utility</name>
  <files>
    package.json
    src/assets/mascot.png
    src/assets/logo-white.png
    src/styles/globals.css
  </files>
  <action>
1. Install lucide-react:
   ```
   bun add lucide-react
   ```

2. Create src/assets/ directory and copy image files from original SPA:
   ```
   mkdir -p src/assets
   cp /home/hybridz/Projects/deep-tech-nexus/src/assets/mascot.png src/assets/mascot.png
   cp /home/hybridz/Projects/deep-tech-nexus/src/assets/logo-white.png src/assets/logo-white.png
   ```

3. Add `font-mono-accent` utility class to `src/styles/globals.css` in the `@layer utilities` block. This class is used throughout the original SPA for monospace accent text (eyebrow labels, taglines, small caps). Add it after the existing utility classes:
   ```css
   .font-mono-accent {
     font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
     letter-spacing: 0.05em;
   }
   ```
   This uses the system monospace font stack. Do NOT install a new font package for this.

4. Run `bun run check` to verify no Biome issues after changes.
  </action>
  <verify>
- `bun run check` passes with zero errors
- `ls src/assets/mascot.png src/assets/logo-white.png` shows both files exist
- `bunx biome check src/styles/globals.css` passes
- `bun pm ls | grep lucide-react` shows lucide-react is installed
  </verify>
  <done>lucide-react installed, mascot.png and logo-white.png copied to src/assets/, font-mono-accent utility added to globals.css</done>
</task>

<task type="auto">
  <name>Task 2: Create complete translation files and update page entry point</name>
  <files>
    messages/es.json
    messages/en.json
    src/app/[locale]/page.tsx
  </files>
  <action>
**Part A: Translation files**

Replace the contents of `messages/es.json` and `messages/en.json` with the COMPLETE translation key set extracted from the original SPA's LanguageContext.tsx. Use nested `landing.*` namespace structure.

CRITICAL: Both files MUST have identical key structure. Only VALUES differ between languages.

The complete key inventory from LanguageContext.tsx (64 keys total):

**Structure:**
```
landing.
  banner.text, banner.link
  nav.about, nav.programs, nav.community, nav.events, nav.cta
  hero.eyebrow, hero.headline, hero.subtitle, hero.cta1, hero.cta2
  traction.community, traction.summit, traction.applicants, traction.fellows
  houses.title, houses.subtitle, houses.ai.desc, houses.biotech.desc, houses.hardware.desc
  programs.title, programs.subtitle
  programs.preincubation.badge, programs.preincubation.duration, programs.preincubation.b1, programs.preincubation.b2, programs.preincubation.b3, programs.preincubation.b4, programs.preincubation.cta
  programs.fellowship.badge, programs.fellowship.duration, programs.fellowship.b1, programs.fellowship.b2, programs.fellowship.b3, programs.fellowship.b4, programs.fellowship.cta
  events.title, events.subtitle
  events.spechack.tagline, events.spechack.desc, events.spechack.register, events.spechack.ambassador
  community.title, community.subtitle, community.link
  partners.title
  intent.headline, intent.subtitle
  intent.build, intent.collaborate, intent.connect
  intent.name, intent.email, intent.workEmail
  intent.build.submit, intent.collaborate.submit, intent.connect.submit
  intent.build.helper, intent.collaborate.helper, intent.connect.helper
  intent.success, intent.questions
  footer.tagline, footer.email, footer.location, footer.rights
```

**IMPORTANT -- keys NOT in original LanguageContext that need to be ADDED:**
- `programs.preincubation.duration` -- ES: "12 semanas", EN: "12 weeks" (hardcoded in original SPA as "12 weeks")
- `programs.fellowship.duration` -- ES: "6 meses", EN: "6 months" (hardcoded as "6 months")
- `programs.fellowship.b1` through `programs.fellowship.b4` -- The original SPA hardcoded these in English only. Add both ES and EN:
  - b1: ES "Financiamiento sin equity" / EN "Equity-free funding"
  - b2: ES "Acceso a lab y workspace" / EN "Lab & workspace access"
  - b3: ES "Presentaciones a inversores" / EN "Investor introductions"
  - b4: ES "Red internacional" / EN "International network"
- `footer.email` -- ES/EN: "hola@404techfound.com" (hardcoded in original)
- `footer.location` -- ES/EN: "Lima, Peru" (hardcoded in original, use flag emoji in both)

**Spanish values (es.json):** Copy EXACTLY from LanguageContext.tsx `es` values.
**English values (en.json):** Copy EXACTLY from LanguageContext.tsx `en` values.

Remove the Phase 1 test keys (`landing.hero.title`, `landing.phase1.status`) -- they are replaced by the real content.

Format with tabs and use Biome formatting convention (tabs, double quotes in source but JSON uses double quotes naturally).

**Part B: Update page.tsx**

Replace `src/app/[locale]/page.tsx` with the landing page composition. It must:
1. Import `setRequestLocale` from `next-intl/server`
2. Import all 11 section components from `./_components/` directory
3. Be an async Server Component that awaits params and calls setRequestLocale
4. Compose sections in this exact order (matching original SPA Index.tsx):
   - AnnouncementBanner
   - Navbar
   - Hero
   - TractionBar
   - Houses
   - Programs
   - Events
   - Community
   - Partners
   - IntentCTA
   - Footer
5. Wrap in `<main className="min-h-screen bg-background text-foreground">`
6. Type the params as `Promise<{ locale: string }>` (Next.js 16 convention)

NOTE: The imported components will NOT exist yet (they are created in Plans 02-02 and 02-03). This is intentional -- the page establishes the composition. TypeScript will show import errors until the components are created. This is expected and acceptable.

Use Biome formatting: tabs, double quotes.
  </action>
  <verify>
- `cat messages/es.json | bun -e "const j = await Bun.file('messages/es.json').json(); const keys = JSON.stringify(j); console.log(keys.includes('eyebrow'), keys.includes('spechack'), keys.includes('intent'), keys.includes('footer'))"` -- all print true
- `cat messages/en.json | bun -e "const j = await Bun.file('messages/en.json').json(); const keys = JSON.stringify(j); console.log(keys.includes('eyebrow'), keys.includes('spechack'), keys.includes('intent'), keys.includes('footer'))"` -- all print true
- `bun -e "const es = await Bun.file('messages/es.json').json(); const en = await Bun.file('messages/en.json').json(); console.log(JSON.stringify(Object.keys(es)) === JSON.stringify(Object.keys(en)))"` -- prints true (same top-level structure)
- `bunx biome check messages/es.json messages/en.json src/app/\\[locale\\]/page.tsx` passes
- page.tsx contains imports for all 11 sections: AnnouncementBanner, Navbar, Hero, TractionBar, Houses, Programs, Events, Community, Partners, IntentCTA, Footer
  </verify>
  <done>
- messages/es.json contains all 64+ translation keys in nested landing.* format with Spanish values
- messages/en.json contains identical key structure with English values
- No Phase 1 test keys remain (hero.title, phase1.status removed)
- page.tsx composes all 11 sections in correct order as async Server Component
  </done>
</task>

</tasks>

<verification>
1. `bun run check` passes (Biome clean)
2. lucide-react is in package.json dependencies
3. src/assets/ contains mascot.png and logo-white.png
4. messages/es.json has nested landing.hero, landing.houses, landing.programs, landing.events, landing.community, landing.partners, landing.intent, landing.footer sections
5. messages/en.json has identical structure to es.json
6. src/app/[locale]/page.tsx imports all 11 section components
7. globals.css contains .font-mono-accent utility
</verification>

<success_criteria>
- All ~64 translation keys from LanguageContext.tsx are present in both es.json and en.json
- Both message files have identical key structure (keys match, only values differ)
- page.tsx is a valid async Server Component composing all 11 sections in correct order
- lucide-react installed, image assets copied, CSS utility added
- No 'use client' directive in page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content-migration/02-01-SUMMARY.md`
</output>
