---
phase: 02-static-content-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/[locale]/_components/Hero.tsx
  - src/app/[locale]/_components/Houses.tsx
  - src/app/[locale]/_components/Programs.tsx
  - src/app/[locale]/_components/Events.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section renders mascot image, Orbitron headline, purple glow, eyebrow text, and two CTA buttons from translation keys"
    - "Houses section renders 3 house cards (AI/Biotech/Hardware) with correct icons (Cpu/Dna/Cog), house colors, and translated descriptions"
    - "Programs section renders Pre-Incubation (OPEN, 12 weeks, 4 benefits) and Fellowship (COMING SOON, 6 months, 4 benefits) cards"
    - "Events section renders SpecHack featured event (Jun 19-28, 2026) with register/ambassador links, plus 2 secondary events"
    - "Zero 'use client' directives in any of these 4 components"
  artifacts:
    - path: "src/app/[locale]/_components/Hero.tsx"
      provides: "Hero section with mascot, headline, CTAs"
      contains: "getTranslations"
      min_lines: 40
    - path: "src/app/[locale]/_components/Houses.tsx"
      provides: "Houses section with 3 house cards"
      contains: "getTranslations"
      min_lines: 30
    - path: "src/app/[locale]/_components/Programs.tsx"
      provides: "Programs section with pre-incubation and fellowship"
      contains: "getTranslations"
      min_lines: 40
    - path: "src/app/[locale]/_components/Events.tsx"
      provides: "Events section with SpecHack featured + 2 secondary events"
      contains: "getTranslations"
      min_lines: 40
  key_links:
    - from: "src/app/[locale]/_components/Hero.tsx"
      to: "messages/*.json"
      via: "getTranslations('landing.hero')"
      pattern: "getTranslations\\([\"']landing\\.hero[\"']\\)"
    - from: "src/app/[locale]/_components/Houses.tsx"
      to: "messages/*.json"
      via: "getTranslations('landing.houses')"
      pattern: "getTranslations\\([\"']landing\\.houses[\"']\\)"
    - from: "src/app/[locale]/_components/Hero.tsx"
      to: "src/assets/mascot.png"
      via: "next/image import"
      pattern: "import.*mascot.*from.*assets"
---

<objective>
Create the 4 primary content sections (Hero, Houses, Programs, Events) as async Server Components with next-intl translations, preserving the original SPA's JSX structure and Tailwind classes while stripping all client-side features.

Purpose: These are the most visually impactful sections of the landing page -- Hero is above-the-fold, Houses explains the core value proposition, Programs shows offerings, Events promotes SpecHack.
Output: 4 Server Component files in src/app/[locale]/_components/ with full translation integration.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-content-migration/02-RESEARCH.md
@.planning/phases/02-static-content-migration/02-01-SUMMARY.md

# Current project files for reference
@src/styles/globals.css
@messages/es.json

# Original SPA components to migrate (READ THESE for exact JSX structure and Tailwind classes)
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Hero.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Houses.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Programs.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Events.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero and Houses Server Components</name>
  <files>
    src/app/[locale]/_components/Hero.tsx
    src/app/[locale]/_components/Houses.tsx
  </files>
  <action>
Create the `src/app/[locale]/_components/` directory if it does not exist.

**Hero.tsx** -- Migrate from original `/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Hero.tsx`:

1. Create as async named export: `export async function Hero()`
2. Import `getTranslations` from `next-intl/server`, `Image` from `next/image`, mascot from `@/assets/mascot.png`
3. Call `const t = await getTranslations("landing.hero")`
4. Preserve the EXACT JSX structure from original:
   - `<section className="relative min-h-screen flex items-center justify-center overflow-hidden pt-16">`
   - Background grid div with inline style (linear-gradient pattern at 60px, opacity-[0.04])
   - Container with flex-col lg:flex-row layout
   - Left column: eyebrow (font-mono-accent text-xs tracking-[0.3em] text-primary), headline (font-orbitron text-4xl md:text-5xl lg:text-6xl font-bold text-glow-purple), subtitle (text-lg md:text-xl text-muted-foreground), two CTA buttons
   - Right column: mascot image

5. CHANGES from original:
   - REMOVE all `<motion.p>`, `<motion.h1>`, `<motion.div>`, `<motion.img>` -- replace with `<p>`, `<h1>`, `<div>`, `<Image>` respectively
   - REMOVE all `initial`, `animate`, `transition`, `whileInView`, `viewport` props
   - REMOVE `import { motion } from "framer-motion"`
   - REMOVE `import { useLanguage }` and `const { t } = useLanguage()`
   - REMOVE `const scrollTo` function and all onClick handlers
   - REPLACE `<button onClick={() => scrollTo("intent-cta")}` with `<a href="#intent-cta"` (keep same classes)
   - REPLACE `<button onClick={() => scrollTo("houses")}` with `<a href="#houses"` (keep same classes)
   - REPLACE `<motion.img src={mascot} ... loading="lazy" />` with `<Image src={mascotImage} alt="Tardi, the 404 Tech Found mascot -- a cyberpunk tardigrade in a lab coat" width={384} height={384} priority className="w-64 md:w-80 lg:w-96 drop-shadow-[0_0_30px_hsl(262_85%_50%/0.4)]" />`
   - REPLACE `t("hero.eyebrow")` with `t("eyebrow")` (namespace already scoped to landing.hero)
   - Same for headline, subtitle, cta1, cta2

6. Use Biome formatting: tabs, double quotes.

**Houses.tsx** -- Migrate from original `/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Houses.tsx`:

1. Create as async named export: `export async function Houses()`
2. Import `getTranslations` from `next-intl/server`, icons `{ Cpu, Dna, Cog }` from `lucide-react`
3. Call `const t = await getTranslations("landing.houses")`
4. Define houses array with key, icon, tagline, colorClass, glowClass, bgClass (same data as original)
5. Preserve EXACT JSX structure:
   - `<section id="houses" className="py-24">`
   - Header with font-orbitron title + subtitle
   - `grid md:grid-cols-3 gap-6` for house cards
   - Each card: rounded-xl border p-8, Icon, tagline, translated desc

6. CHANGES from original:
   - REMOVE all `<motion.div>` -- replace with `<div>`
   - REMOVE framer-motion import
   - REMOVE useLanguage import/usage
   - REPLACE `t("houses.title")` with `t("title")` (namespace scoped)
   - REPLACE `t(house.descKey)` with `t(\`${house.key}.desc\`)` (use key from house object + .desc)
   - Keep ALL Tailwind classes including hover:scale-[1.02] transition-transform (CSS hover still works in RSC)
  </action>
  <verify>
- `bunx biome check "src/app/[locale]/_components/Hero.tsx" "src/app/[locale]/_components/Houses.tsx"` passes
- Neither file contains `'use client'`
- Neither file imports from `framer-motion`
- Neither file uses `useLanguage`, `useState`, `useEffect`, or any React hooks
- Hero.tsx contains `getTranslations("landing.hero")` and `import Image from "next/image"`
- Houses.tsx contains `getTranslations("landing.houses")` and imports `Cpu, Dna, Cog` from lucide-react
  </verify>
  <done>Hero.tsx renders mascot image with next/image optimization, Orbitron headline with purple glow, eyebrow text, subtitle, and two anchor-link CTAs. Houses.tsx renders 3 house cards with Cpu/Dna/Cog icons, house-specific colors, and translated descriptions. Both are pure async Server Components.</done>
</task>

<task type="auto">
  <name>Task 2: Create Programs and Events Server Components</name>
  <files>
    src/app/[locale]/_components/Programs.tsx
    src/app/[locale]/_components/Events.tsx
  </files>
  <action>
**Programs.tsx** -- Migrate from original `/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Programs.tsx`:

1. Create as async named export: `export async function Programs()`
2. Import `getTranslations` from `next-intl/server`, `{ Check }` from `lucide-react`
3. Call `const t = await getTranslations("landing.programs")`
4. Build benefits arrays using translation keys:
   ```
   const preincBenefits = [t("preincubation.b1"), t("preincubation.b2"), t("preincubation.b3"), t("preincubation.b4")];
   const fellowshipBenefits = [t("fellowship.b1"), t("fellowship.b2"), t("fellowship.b3"), t("fellowship.b4")];
   ```
5. Preserve EXACT JSX structure:
   - `<section id="programs" className="py-24">`
   - Header with font-orbitron title + subtitle
   - `grid md:grid-cols-2 gap-8 max-w-4xl mx-auto`
   - Pre-Incubation card: badge (bg-house-biotech/20 text-house-biotech), duration, "Pre-Incubation" title, benefits list with Check icons, CTA button (gradient-purple)
   - Fellowship card: badge (bg-primary/20 text-primary), duration, "404 Founders Fellowship" title, benefits list with Check icons, disabled CTA button

6. CHANGES from original:
   - REMOVE all `<motion.div>` -- replace with `<div>`
   - REMOVE framer-motion import, useLanguage import
   - REPLACE hardcoded "12 weeks" with `t("preincubation.duration")`
   - REPLACE hardcoded "6 months" with `t("fellowship.duration")`
   - REPLACE hardcoded English fellowship benefits with translation calls
   - REPLACE `t("programs.preincubation.badge")` with `t("preincubation.badge")` (namespace scoped)
   - REPLACE `<button>` elements with `<a href="#intent-cta">` for the CTA (Pre-Incubation) and keep `<button disabled>` for Fellowship
   - Keep ALL Tailwind classes exactly as original

**Events.tsx** -- Migrate from original `/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Events.tsx`:

1. Create as async named export: `export async function Events()`
2. Import `getTranslations` from `next-intl/server`, `{ Calendar, MapPin }` from `lucide-react`
3. Call `const t = await getTranslations("landing.events")`
4. Preserve EXACT JSX structure:
   - `<section id="events" className="py-24">`
   - Header with font-orbitron title + subtitle
   - Featured SpecHack card: `rounded-xl border border-primary/30 bg-primary/5 p-8 md:p-12 mb-8 glow-border-ai max-w-4xl mx-auto` with inline boxShadow style
   - SpecHack details: "FEATURED EVENT" label, "404 SpecHack" title, tagline, Calendar icon with "Jun 19-28, 2026", MapPin icon with "Global - Hybrid", description, two action links (register + ambassador)
   - Secondary events grid: `grid md:grid-cols-2 gap-6 max-w-4xl mx-auto`
   - Two secondary event cards: "Deeptech Summit 2026" (Lima, Peru, SEP 2026) and "International Demo Day" (Virtual, DEC 2026)

5. CHANGES from original:
   - REMOVE all `<motion.div>` -- replace with `<div>`
   - REMOVE framer-motion import, useLanguage import
   - REPLACE `t("events.title")` with `t("title")` (namespace scoped)
   - Keep the `<a>` elements for SpecHack links exactly as-is (they're already anchors with href, not onClick)
   - Keep ALL Tailwind classes and inline styles exactly as original
   - Secondary events data can remain as inline array (they are not translated -- names/dates are proper nouns)
  </action>
  <verify>
- `bunx biome check "src/app/[locale]/_components/Programs.tsx" "src/app/[locale]/_components/Events.tsx"` passes
- Neither file contains `'use client'`
- Neither file imports from `framer-motion`
- Programs.tsx uses `t("preincubation.b1")` pattern (not hardcoded English strings for fellowship)
- Programs.tsx uses `t("preincubation.duration")` and `t("fellowship.duration")` (not hardcoded "12 weeks" / "6 months")
- Events.tsx contains "Jun 19-28, 2026" and "404 SpecHack"
- Events.tsx has `<a href="https://spechack.404tf.com"` for register/ambassador links
  </verify>
  <done>Programs.tsx renders Pre-Incubation (OPEN badge, 12 weeks, 4 translated benefits) and Fellowship (COMING SOON, 6 months, 4 translated benefits) cards. Events.tsx renders featured SpecHack event with date/location metadata and register/ambassador links, plus 2 secondary events. Both are pure async Server Components with zero client-side code.</done>
</task>

</tasks>

<verification>
1. `bunx biome check src/app/\\[locale\\]/_components/` passes on all 4 files
2. `grep -r "'use client'" src/app/\\[locale\\]/_components/` returns no results
3. `grep -r "framer-motion" src/app/\\[locale\\]/_components/` returns no results
4. `grep -r "useLanguage\|useState\|useEffect" src/app/\\[locale\\]/_components/` returns no results
5. All 4 files use `getTranslations` from `next-intl/server`
6. Hero.tsx uses `next/image` for mascot
7. Houses.tsx uses Cpu, Dna, Cog from lucide-react
8. Programs.tsx uses Check from lucide-react
9. Events.tsx uses Calendar, MapPin from lucide-react
</verification>

<success_criteria>
- 4 Server Component files created: Hero.tsx, Houses.tsx, Programs.tsx, Events.tsx
- All use async function + getTranslations pattern
- Zero 'use client' directives
- Zero framer-motion imports
- All Tailwind classes preserved from original SPA
- Hero uses next/image with priority for above-fold mascot
- All translation keys reference the correct namespace (no double-nesting like t("hero.headline") when namespace is already "landing.hero")
- Responsive classes preserved: flex-col lg:flex-row, md:grid-cols-3, md:grid-cols-2, etc.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content-migration/02-02-SUMMARY.md`
</output>
