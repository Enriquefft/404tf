---
phase: 02-static-content-migration
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/[locale]/_components/Community.tsx
  - src/app/[locale]/_components/Partners.tsx
  - src/app/[locale]/_components/Footer.tsx
  - src/app/[locale]/_components/AnnouncementBanner.tsx
  - src/app/[locale]/_components/Navbar.tsx
  - src/app/[locale]/_components/TractionBar.tsx
  - src/app/[locale]/_components/IntentCTA.tsx
autonomous: true

must_haves:
  truths:
    - "Community section renders 3 fellow profiles (Maria Chen, Carlos Medina, Ana Torres) with house badges and startup descriptions"
    - "Partners section renders 8 partner name placeholders in a grayscale grid"
    - "Footer renders logo, nav links, contact email (hola@404techfound.com), social links (LinkedIn, X, Instagram, Discord), and copyright"
    - "Navbar renders as a static header with logo, nav links, SpecHack link, and CTA -- no scroll detection or mobile menu toggle (Phase 3)"
    - "AnnouncementBanner renders SpecHack banner text with register link -- no dismiss button behavior (Phase 3)"
    - "TractionBar renders 4 stats with static target numbers -- no count-up animation (Phase 3)"
    - "IntentCTA renders 3 intent cards (Build/Collaborate/Connect) with headline -- no form or selection behavior (Phase 3)"
    - "Zero 'use client' directives in any of these 7 components"
  artifacts:
    - path: "src/app/[locale]/_components/Community.tsx"
      provides: "Fellow profiles section"
      contains: "getTranslations"
    - path: "src/app/[locale]/_components/Partners.tsx"
      provides: "Partner logo grid placeholder"
      contains: "getTranslations"
    - path: "src/app/[locale]/_components/Footer.tsx"
      provides: "Footer with contact info and social links"
      contains: "getTranslations"
    - path: "src/app/[locale]/_components/AnnouncementBanner.tsx"
      provides: "Static SpecHack announcement banner"
      contains: "getTranslations"
    - path: "src/app/[locale]/_components/Navbar.tsx"
      provides: "Static navigation bar"
      contains: "getTranslations"
    - path: "src/app/[locale]/_components/TractionBar.tsx"
      provides: "Static traction statistics bar"
      contains: "getTranslations"
    - path: "src/app/[locale]/_components/IntentCTA.tsx"
      provides: "Static intent CTA section placeholder"
      contains: "getTranslations"
  key_links:
    - from: "src/app/[locale]/_components/Footer.tsx"
      to: "src/assets/logo-white.png"
      via: "next/image import"
      pattern: "import.*logo.*from.*assets"
    - from: "src/app/[locale]/_components/Navbar.tsx"
      to: "src/assets/logo-white.png"
      via: "next/image import"
      pattern: "import.*logo.*from.*assets"
    - from: "src/app/[locale]/_components/Footer.tsx"
      to: "messages/*.json"
      via: "getTranslations for nav and footer keys"
      pattern: "getTranslations\\([\"']landing\\."
---

<objective>
Create the remaining 7 section components: Community, Partners, Footer as fully-implemented Server Components, plus 4 static placeholder sections (AnnouncementBanner, Navbar, TractionBar, IntentCTA) that render correct HTML structure but defer interactive behavior to Phase 3.

Purpose: Complete the full landing page by filling in all remaining sections. The 4 placeholder sections render the right visual structure and translation keys so the page looks complete, but their interactive features (dismiss, scroll detection, animations, form) come in Phase 3.
Output: 7 Server Component files completing the landing page.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-content-migration/02-RESEARCH.md
@.planning/phases/02-static-content-migration/02-01-SUMMARY.md

# Current project files
@src/styles/globals.css
@messages/es.json

# Original SPA components to migrate
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Community.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Partners.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Footer.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/AnnouncementBanner.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/Navbar.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/TractionBar.tsx
@/home/hybridz/Projects/deep-tech-nexus/src/components/landing/IntentCTA.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Community, Partners, and Footer Server Components</name>
  <files>
    src/app/[locale]/_components/Community.tsx
    src/app/[locale]/_components/Partners.tsx
    src/app/[locale]/_components/Footer.tsx
  </files>
  <action>
**Community.tsx** -- Migrate from original Community.tsx:

1. Create as async named export: `export async function Community()`
2. Import `getTranslations` from `next-intl/server`
3. Call `const t = await getTranslations("landing.community")`
4. Define fellows array inline (same data as original -- these are display names, not translated):
   ```
   const fellows = [
     { name: "Maria Chen", startup: "NeuroSpec AI", house: "AI", houseColor: "bg-house-ai", desc: "Computer vision for agricultural pest detection" },
     { name: "Carlos Medina", startup: "BioSynth Labs", house: "Biotech", houseColor: "bg-house-biotech", desc: "Synthetic biology for sustainable materials" },
     { name: "Ana Torres", startup: "MechaPrecision", house: "Hardware", houseColor: "bg-house-hardware", desc: "Low-cost medical robotics for rural clinics" },
   ];
   ```
5. Preserve EXACT JSX structure from original:
   - `<section id="community" className="py-24">`
   - Header: font-orbitron title + subtitle
   - `grid md:grid-cols-3 gap-6 max-w-4xl mx-auto mb-10`
   - Each fellow card: `rounded-xl border border-border bg-card p-6 text-center`
   - Avatar placeholder: `<div className="w-20 h-20 rounded-full bg-muted mx-auto mb-4" />`
   - House badge: `<span className={\`${fellow.houseColor} text-background text-[10px] font-bold px-2 py-0.5 rounded-full\`}>`
   - Name, startup, desc text
   - Community link at bottom

6. CHANGES from original:
   - REMOVE all `<motion.div>` -- replace with `<div>`
   - REMOVE framer-motion import, useLanguage import
   - REPLACE `<button>` for community link with `<a href="#community">` or just a `<span>` (no scroll behavior in Phase 2)
   - Use `t("title")`, `t("subtitle")`, `t("link")` (namespace scoped)

**Partners.tsx** -- Migrate from original Partners.tsx:

1. Create as async named export: `export async function Partners()`
2. Import `getTranslations` from `next-intl/server`
3. Call `const t = await getTranslations("landing.partners")`
4. Define partner names inline (same data as original):
   ```
   const partnerNames = ["TechVentures", "Latam Labs", "ScienceCo", "InnovateX", "DeepFund", "BioForward", "NanoWorks", "FutureScale"];
   ```
5. Preserve EXACT JSX structure:
   - `<section className="py-24">` (no id in original)
   - Header: font-orbitron title only (no subtitle)
   - `grid grid-cols-2 md:grid-cols-4 gap-4 max-w-3xl mx-auto`
   - Each partner: `border border-border rounded-lg p-6 flex items-center justify-center grayscale hover:grayscale-0 opacity-50 hover:opacity-100 transition-all duration-300`
   - Partner name: `font-orbitron text-xs text-muted-foreground`

6. CHANGES: Remove motion.div, framer-motion, useLanguage. Use `t("title")`.

**Footer.tsx** -- Migrate from original Footer.tsx:

1. Create as async named export: `export async function Footer()`
2. Import `getTranslations` from `next-intl/server`, `Image` from `next/image`, logo from `@/assets/logo-white.png`
3. Need BOTH nav and footer translations. Call:
   ```
   const t = await getTranslations("landing.footer");
   const tNav = await getTranslations("landing.nav");
   ```
4. Build nav links array using tNav:
   ```
   const navLinks = [
     { label: tNav("about"), target: "houses" },
     { label: tNav("programs"), target: "programs" },
     { label: tNav("community"), target: "community" },
     { label: tNav("events"), target: "events" },
   ];
   ```
5. Preserve EXACT JSX structure:
   - `<footer className="border-t border-border py-16">`
   - Container with `flex flex-col md:flex-row items-start justify-between gap-10`
   - Left: Logo image + tagline
   - Center: Nav links
   - Right: Email (mailto link), location, social links (LinkedIn, X, Instagram, Discord)
   - Bottom: copyright with `mt-10 pt-6 border-t border-border text-center`

6. CHANGES from original:
   - REMOVE useLanguage, scrollTo function
   - REPLACE `<img src={logoWhite} alt="404 Tech Found" className="h-8 mb-3" loading="lazy" />` with `<Image src={logoImage} alt="404 Tech Found" width={120} height={32} className="h-8 w-auto mb-3" />`
   - REPLACE `<button onClick={() => scrollTo(link.target)}>` with `<a href={\`#${link.target}\`}>` for nav links (anchor links work without JS)
   - Use `t("tagline")`, `t("email")`, `t("location")`, `t("rights")` for footer content
   - Social links remain as `<a href="#">` placeholders (real URLs are Phase 4/5 concern)
  </action>
  <verify>
- `bunx biome check "src/app/[locale]/_components/Community.tsx" "src/app/[locale]/_components/Partners.tsx" "src/app/[locale]/_components/Footer.tsx"` passes
- None of the 3 files contain `'use client'`
- None import from `framer-motion`
- Footer.tsx imports `Image` from `next/image` and logo from `@/assets/logo-white.png`
- Footer.tsx contains `mailto:hola@404techfound.com`
- Footer.tsx contains "LinkedIn", "X", "Instagram", "Discord"
- Community.tsx contains "Maria Chen", "Carlos Medina", "Ana Torres"
- Partners.tsx contains "TechVentures" and maps 8 partner names
  </verify>
  <done>Community.tsx renders 3 fellow profiles with house badges and avatar placeholders. Partners.tsx renders 8 partner name placeholders in a responsive grayscale grid. Footer.tsx renders logo, nav links as anchor links, contact email, location, social links, and copyright. All are pure async Server Components.</done>
</task>

<task type="auto">
  <name>Task 2: Create static placeholder sections for Navbar, AnnouncementBanner, TractionBar, IntentCTA</name>
  <files>
    src/app/[locale]/_components/AnnouncementBanner.tsx
    src/app/[locale]/_components/Navbar.tsx
    src/app/[locale]/_components/TractionBar.tsx
    src/app/[locale]/_components/IntentCTA.tsx
  </files>
  <action>
These 4 components are STATIC PLACEHOLDERS for Phase 2. They render the correct HTML structure and translation keys but defer all interactive behavior (scroll detection, dismiss, animations, form submission) to Phase 3. Phase 3 will convert these to Client Components by adding 'use client' and interactivity.

**AnnouncementBanner.tsx** -- Static version of original:

1. Create as async named export: `export async function AnnouncementBanner()`
2. Import `getTranslations` from `next-intl/server`
3. Call `const t = await getTranslations("landing.banner")`
4. Render the banner ALWAYS visible (no dismiss state in Phase 2):
   ```tsx
   <div className="gradient-purple text-primary-foreground py-2.5 px-4 text-center text-sm font-medium relative z-50">
     <span>{t("text")} </span>
     <a href="https://spechack.404tf.com" target="_blank" rel="noopener"
       className="underline underline-offset-2 font-semibold hover:opacity-80 transition-opacity">
       {t("link")}
     </a>
     {/* Dismiss button rendered but non-functional until Phase 3 adds client interactivity */}
   </div>
   ```
5. Do NOT render the X dismiss button -- it requires useState which needs 'use client'. Phase 3 will add it.

**Navbar.tsx** -- Static version of original:

1. Create as async named export: `export async function Navbar()`
2. Import `getTranslations` from `next-intl/server`, `Image` from `next/image`, logo from `@/assets/logo-white.png`
3. Call:
   ```
   const t = await getTranslations("landing.nav");
   ```
4. Build nav links: about->houses, programs->programs, community->community, events->events
5. Render as a STATIC fixed header (always showing the scrolled/opaque style since we cannot detect scroll):
   ```tsx
   <nav className="fixed top-0 left-0 right-0 z-40 bg-background/80 backdrop-blur-xl border-b border-border">
   ```
6. Desktop nav only (hidden on mobile in Phase 2, mobile menu requires useState):
   - Logo image using next/image
   - Nav links as `<a href={\`#${link.target}\`}>` (anchor links, not buttons with onClick)
   - SpecHack link with "NEW" badge (same markup as original)
   - Language switcher: render a static placeholder showing "ES | EN" (no toggle behavior)
   - CTA button as `<a href="#intent-cta">`
7. Mobile toggle: render a placeholder `<div className="md:hidden p-2">` with a menu icon but NO onClick handler. The mobile menu itself is NOT rendered (it requires useState). Just the hamburger icon visible.
8. Import `{ Menu }` from `lucide-react` for the hamburger icon.

**TractionBar.tsx** -- Static version of original:

1. Create as async named export: `export async function TractionBar()`
2. Import `getTranslations` from `next-intl/server`
3. Call `const t = await getTranslations("landing.traction")`
4. Define stats with STATIC target values (no count-up animation):
   ```
   const stats = [
     { value: "400+", label: t("community") },
     { value: "250+", label: t("summit") },
     { value: "92+", label: t("applicants") },
     { value: "15", label: t("fellows") },
   ];
   ```
5. Render with same layout as original:
   ```tsx
   <section className="py-16 border-y border-border">
     <div className="container mx-auto px-4">
       <div className="grid grid-cols-2 md:grid-cols-4 gap-8">
         {stats.map((stat, i) => (
           <div key={i} className="text-center">
             <p className="font-orbitron text-4xl md:text-5xl font-bold text-primary text-glow-purple">
               {stat.value}
             </p>
             <p className="text-sm text-muted-foreground mt-2">{stat.label}</p>
           </div>
         ))}
       </div>
     </div>
   </section>
   ```
6. Show final numbers directly (not 0). The count-up animation is Phase 3.

**IntentCTA.tsx** -- Static placeholder of original:

1. Create as async named export: `export async function IntentCTA()`
2. Import `getTranslations` from `next-intl/server`
3. Call `const t = await getTranslations("landing.intent")`
4. Render the section with:
   - `<section id="intent-cta" className="py-24 gradient-purple relative overflow-hidden">`
   - Decorative grid overlay (same inline style as original)
   - Headline: `t("headline")` with font-orbitron text-3xl md:text-4xl font-bold text-primary-foreground
   - Subtitle: `t("subtitle")`
   - Three intent cards rendered as static divs (NOT buttons, no onClick):
     ```
     const intents = [
       { key: "build", emoji: "🔬" },
       { key: "collaborate", emoji: "🤝" },
       { key: "connect", emoji: "🌐" },
     ];
     ```
   - Each card: `flex-1 rounded-xl border-2 border-white/20 bg-white/10 p-6 text-center`
   - Show emoji + translated intent name `t(intent.key)`
   - Do NOT render the form -- Phase 3 adds the entire form with TanStack Forms
   - At bottom, render the "Questions?" contact line:
     ```tsx
     <div className="text-center mt-12">
       <p className="text-white/50 text-sm">
         {t("questions")}{" "}
         <a href="mailto:hola@404techfound.com" className="text-white underline underline-offset-2 hover:text-white/80">
           hola@404techfound.com
         </a>
       </p>
     </div>
     ```

Use Biome formatting throughout: tabs, double quotes.
  </action>
  <verify>
- `bunx biome check "src/app/[locale]/_components/AnnouncementBanner.tsx" "src/app/[locale]/_components/Navbar.tsx" "src/app/[locale]/_components/TractionBar.tsx" "src/app/[locale]/_components/IntentCTA.tsx"` passes
- None of the 4 files contain `'use client'`
- None import from `framer-motion`
- None use `useState`, `useEffect`, or any React hooks
- Navbar.tsx imports Image from next/image and logo from @/assets/logo-white.png
- TractionBar.tsx contains "400+", "250+", "92+", "15"
- IntentCTA.tsx contains the 3 intents (build, collaborate, connect) and "mailto:hola@404techfound.com"
- AnnouncementBanner.tsx contains link to "https://spechack.404tf.com"
  </verify>
  <done>
- AnnouncementBanner renders always-visible SpecHack banner (dismiss deferred to Phase 3)
- Navbar renders static fixed header with logo, anchor nav links, SpecHack badge, language placeholder, CTA (scroll detection + mobile menu deferred to Phase 3)
- TractionBar renders 4 stats with final numbers displayed statically (count-up animation deferred to Phase 3)
- IntentCTA renders headline, subtitle, 3 intent cards, and contact line (form + card selection deferred to Phase 3)
- All 4 are pure async Server Components with zero client-side code
  </done>
</task>

</tasks>

<verification>
1. All 7 files pass `bunx biome check`
2. `grep -r "'use client'" src/app/\\[locale\\]/_components/` returns ZERO results across ALL 11 component files
3. `grep -r "framer-motion" src/app/\\[locale\\]/_components/` returns ZERO results
4. `grep -r "useState\|useEffect\|useContext" src/app/\\[locale\\]/_components/` returns ZERO results
5. All 7 files use `getTranslations` from `next-intl/server`
6. Footer.tsx and Navbar.tsx both use next/image for logo
7. `bun run build` completes without errors (full page renders with all 11 sections)
8. Visiting `/es` shows full Spanish landing page
9. Visiting `/en` shows full English landing page
</verification>

<success_criteria>
- 7 Server Component files created: Community, Partners, Footer, AnnouncementBanner, Navbar, TractionBar, IntentCTA
- Combined with Plan 02-02's 4 files, all 11 sections exist
- Zero 'use client' directives across entire _components/ directory
- `bun run build` succeeds
- `/es` renders complete Spanish landing page with all 11 sections visible
- `/en` renders complete English landing page with correct translations
- Mobile-responsive: all Tailwind responsive classes preserved from original SPA
- Static placeholders clearly defer interactive behavior to Phase 3 (comments in code)
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content-migration/02-03-SUMMARY.md`
</output>
