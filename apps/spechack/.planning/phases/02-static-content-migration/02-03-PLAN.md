---
phase: 02-static-content-migration
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/[locale]/_components/Judging.tsx
  - src/app/[locale]/_components/Hubs.tsx
  - src/app/[locale]/_components/Sponsors.tsx
autonomous: true

must_haves:
  truths:
    - "User sees Judging section with 5 criteria progress bars (static widths) and 5 prize categories"
    - "User sees Hubs section with city map showing 6 cities (1 confirmed), ambassador pitch text, and ambassador application form placeholder"
    - "User sees Sponsors section with 3 metrics, 3 value props, deck download CTA, and book-a-call CTA"
  artifacts:
    - path: "src/app/[locale]/_components/Judging.tsx"
      provides: "Judging criteria bars and prize categories"
      exports: ["Judging"]
    - path: "src/app/[locale]/_components/Hubs.tsx"
      provides: "City map, ambassador pitch, and application form"
      exports: ["Hubs"]
    - path: "src/app/[locale]/_components/Sponsors.tsx"
      provides: "Sponsor metrics, value props, and CTAs"
      exports: ["Sponsors"]
  key_links:
    - from: "src/app/[locale]/_components/Judging.tsx"
      to: "next-intl/server"
      via: "getTranslations for bilingual content"
      pattern: "getTranslations"
    - from: "src/app/[locale]/_components/Hubs.tsx"
      to: "next-intl/server"
      via: "getTranslations for bilingual content"
      pattern: "getTranslations"
    - from: "src/app/[locale]/_components/Sponsors.tsx"
      to: "next-intl/server"
      via: "getTranslations for bilingual content"
      pattern: "getTranslations"
---

<objective>
Create the Judging, Hubs, and Sponsors landing sections as server components with bilingual content.

Purpose: These three sections form the middle content of the landing page. In the blueprint, Judging and Hubs share a two-column layout within one section. All three are pure server components with presentational-only forms (backend logic is Phase 4).

Output: Three server component files ready for page composition in Plan 04.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-content-migration/02-RESEARCH.md
@.planning/phases/02-static-content-migration/02-01-SUMMARY.md

Source components to port from:
@/home/hybridz/Projects/spechack-blueprint/src/components/sections/Judging.tsx
@/home/hybridz/Projects/spechack-blueprint/src/components/sections/Sponsors.tsx

Existing patterns:
@messages/es.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Judging and Hubs server components</name>
  <files>src/app/[locale]/_components/Judging.tsx, src/app/[locale]/_components/Hubs.tsx</files>
  <action>
**Important context:** In the blueprint, Judging and Hubs are combined in one file (Judging.tsx) as a two-column layout. In Phase 2, separate them into two components for better modularity. Plan 04's page.tsx will wrap both in a shared `<section id="prizes">` with a two-column grid. Therefore, **Judging.tsx and Hubs.tsx must render `<div>` root wrappers, NOT `<section>` elements**, since page.tsx provides the parent section wrapper.

**Judging.tsx** — Async SERVER component. Port the left column from blueprint Judging.tsx.

```typescript
import { getTranslations } from "next-intl/server";
export async function Judging() { ... }
```

Structure:
1. Root wrapper: `<div>` (NOT `<section>` -- the parent section with id="prizes" is in page.tsx)
2. Section heading using judging translations
3. **Criteria progress bars** — 5 bars with STATIC widths (no animation, that's Phase 3):
   - Each bar: label (font-mono text-xs text-muted-foreground) + percentage (font-mono text-xs text-primary) on same line, then a full-width track (h-2 bg-muted rounded-full) with a filled portion set via inline `style={{ width: "{pct}%" }}` and `bg-primary rounded-full`.
   - Criteria data is in the component, not translations (percentages are language-independent):
     ```
     const criteria = [
       { labelKey: "criteria0Label", pct: 30 },
       { labelKey: "criteria1Label", pct: 25 },
       { labelKey: "criteria2Label", pct: 20 },
       { labelKey: "criteria3Label", pct: 15 },
       { labelKey: "criteria4Label", pct: 10 },
     ];
     ```
     Use `t(criterion.labelKey)` for the translated label.

4. **Prize list** — 5 prize items:
   - Each: emoji span + label (font-orbitron font-bold text-sm) + description (text-muted-foreground text-xs font-mono)
   - Data structure similar to criteria, using translation keys like `prize0Emoji`, `prize0Label`, `prize0Desc`
   - Emoji values are the same in both languages, so hardcode them in the component:
     ```
     const prizes = [
       { emoji: "🏆", labelKey: "prize0Label", descKey: "prize0Desc" },
       { emoji: "📋", labelKey: "prize1Label", descKey: "prize1Desc" },
       ...
     ];
     ```

5. Pool text (font-mono text-sm text-muted-foreground)
6. Register CTA link: `<a href="#register">` with primary button styling

**Hubs.tsx** — Async SERVER component. Port the right column from blueprint Judging.tsx.

```typescript
import { getTranslations } from "next-intl/server";
import { MapPin } from "lucide-react";
export async function Hubs() { ... }
```

Structure:
1. Root wrapper: `<div>` (NOT `<section>` -- the parent section is in page.tsx, which provides the two-column grid container)
2. Section heading using hubs translations
3. Traction line: font-mono text-xs text-secondary
4. **City map** — Simplified map representation:
   - Container: `relative w-full aspect-[2/1] bg-card border border-border rounded-lg mb-8 overflow-hidden`
   - Blueprint grid overlay: `<div className="absolute inset-0 blueprint-grid opacity-50" />`
   - 6 city pins positioned with absolute + inline styles (left %, top %):
     ```
     const cities = [
       { name: "Lima", confirmed: true, x: 28, y: 65 },
       { name: "Sao Paulo", confirmed: false, x: 38, y: 70 },
       { name: "Mexico City", confirmed: false, x: 18, y: 48 },
       { name: "Bogota", confirmed: false, x: 26, y: 55 },
       { name: "Buenos Aires", confirmed: false, x: 32, y: 82 },
       { name: "Santiago", confirmed: false, x: 27, y: 76 },
     ];
     ```
   - Each pin: `MapPin` icon (lucide-react) + city name label. Confirmed city in `text-spec-green`, unconfirmed in `text-muted-foreground/40`.

5. Ambassador pitch text (text-muted-foreground text-sm)
6. **Ambassador form — PRESENTATIONAL ONLY:**
   - Show the "Apply" CTA button (`type="button"`, non-functional)
   - Below it, show the form fields as a static, always-visible form (simplified from blueprint's toggle behavior — the toggle expand/collapse is Phase 3 animation scope):
     - Actually, keep it simple: just show the CTA button. The expandable form is Phase 3/4 scope. The button is `type="button"` with comment `{/* TODO: Phase 3 — expand form, Phase 4 — Server Action */}`
     - Alternative approach: render the form always visible below the button but with `type="button"` on submit. This matches the requirement LAND-06 "expandable application form" but without the expand behavior. Since the requirement says "expandable", just show the CTA button for now (Phase 3 adds the toggle, Phase 4 makes it functional).

Conventions: Tabs, double quotes, named async exports, no default exports, PascalCase files, `@/` imports. Use lucide-react for MapPin icon.
  </action>
  <verify>
Run `bun run build` — no TypeScript errors. Verify both files exist, are server components (no "use client"), use getTranslations, and export named functions. Verify both components render `<div>` root wrappers (not `<section>`).
  </verify>
  <done>
Judging.tsx renders 5 criteria bars with static widths and 5 prize categories inside a `<div>` wrapper. Hubs.tsx renders city map with 6 positioned pins, ambassador pitch text, and apply CTA button inside a `<div>` wrapper. Both are async server components. Neither uses `<section>` -- the parent section wrapper is provided by page.tsx in Plan 04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Sponsors server component</name>
  <files>src/app/[locale]/_components/Sponsors.tsx</files>
  <action>
**Sponsors.tsx** — Async SERVER component. Port from blueprint Sponsors.tsx.

```typescript
import { getTranslations } from "next-intl/server";
import { ExternalLink } from "lucide-react";
export async function Sponsors() { ... }
```

Structure:
1. Section wrapper: `<section id="sponsors" className="py-24 sm:py-32 px-4 bg-card/50 border-t border-b border-primary/10">`
2. Container: `max-w-4xl mx-auto text-center`
3. Section title (font-orbitron font-bold) + subtitle (text-muted-foreground)
4. **Metrics row** — 3 metrics displayed horizontally:
   - `flex justify-center gap-10 sm:gap-16 mb-10`
   - Each metric: large number (font-orbitron font-extrabold text-2xl sm:text-3xl text-primary) + description label (font-mono text-[11px] text-muted-foreground)
   - Metric values are language-independent (numbers), hardcode in component:
     ```
     const metrics = [
       { value: "1,000+", labelKey: "metricDevs" },
       { value: "5+", labelKey: "metricCities" },
       { value: "10", labelKey: "metricDays" },
     ];
     ```
5. **Value props** — 3 text items in a grid:
   - `grid sm:grid-cols-3 gap-6 mb-10`
   - Each prop: `text-sm text-muted-foreground`
   - Use translation keys: `prop0`, `prop1`, `prop2`

6. **CTA buttons** — Two buttons side by side:
   - `flex flex-col sm:flex-row gap-4 justify-center mb-6`
   - Deck download: outlined button (`border border-border text-foreground`) with ExternalLink icon from lucide-react. `href="#"` placeholder.
   - Book a call: primary button (`bg-primary text-primary-foreground`). `href="#"` placeholder, `target="_blank"`.

7. NGO note text: font-mono text-[11px] text-muted-foreground, max-w-lg mx-auto

Conventions: Tabs, double quotes, named async export, no default export, PascalCase file, `@/` imports.
  </action>
  <verify>
Run `bun run build` — no TypeScript errors. Verify file exists, is a server component, uses getTranslations, and exports named function.
  </verify>
  <done>
Sponsors.tsx renders 3 metrics, 3 value props, deck download CTA with ExternalLink icon, book-a-call CTA, and NGO note. Async server component with getTranslations.
  </done>
</task>

</tasks>

<verification>
1. `bun run build` succeeds with no errors
2. Judging.tsx renders 5 criteria bars (static width) and 5 prizes, wrapped in `<div>` (not `<section>`)
3. Hubs.tsx renders city map with MapPin icons and ambassador CTA, wrapped in `<div>` (not `<section>`)
4. Sponsors.tsx renders metrics, value props, and two CTA buttons
5. All three files are async server components (no "use client")
6. All use getTranslations from next-intl/server
</verification>

<success_criteria>
- Judging section faithfully ports criteria and prizes from blueprint
- Hubs section shows city map with correct pin positions and ambassador pitch
- Sponsors section displays metrics, value props, and CTAs matching blueprint
- Judging and Hubs use `<div>` wrappers (page.tsx provides the parent `<section>`)
- No client-side code in any of these components
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content-migration/02-03-SUMMARY.md`
</output>
