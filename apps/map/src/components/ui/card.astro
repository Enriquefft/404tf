---
type Props = {
	variant?: "default" | "featured" | "compact";
	href?: string;
	class?: string;
};

const { variant = "default", href, class: className = "" } = Astro.props;

const variantStyles: Record<string, string> = {
	default: "bg-card border border-[var(--border-subtle)] p-6",
	featured: "bg-card border border-[var(--primary)] p-8",
	compact: "bg-card border border-[var(--border-subtle)] p-3",
};

const hoverStyles = href ? "hover:border-[var(--border-strong)] hover:bg-popover" : "";

const baseStyles = [variantStyles[variant], hoverStyles, className].filter(Boolean).join(" ");
---

{
	href ? (
		<a
			href={href}
			class={baseStyles}
			style="transition: border-color 200ms ease-out, background-color 200ms ease-out;"
		>
			<slot />
		</a>
	) : (
		<div class={baseStyles}>
			<slot />
		</div>
	)
}
