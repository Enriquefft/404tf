{
	"buildCommand": "cd ../.. && bun run build:map",
	"installCommand": "cd ../.. && bun install",
	"framework": "astro",
	"ignoreCommand": "git diff HEAD^ HEAD --quiet -- . ../../packages/",
	"crons": [
		{
			"path": "/api/health",
			"schedule": "*/5 * * * *"
		}
	]
}
